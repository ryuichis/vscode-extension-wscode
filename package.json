{
  "name": "cerebras-inference",
  "displayName": "Cerebras Inference",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "cerebras-inference.ask",
        "title": "Ask Cerebras Inference"
      },
      {
        "command": "cerebras-inference.openChatPanel",
        "title": "Open Cerebras Inference Chat Panel"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Cerebras Inference",
      "properties": {
        "cerebras-inference.apiKey": {
          "type": "string",
          "default": "",
          "description": "API Key for Cerebras Inference"
        }
      }
    }
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.96.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.16.0"
  },
  "dependencies": {
    "@cerebras/cerebras_cloud_sdk": "^1.15.0"
  }
}
